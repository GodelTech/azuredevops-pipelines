# test-dotnet-dotnet-format.test.yml

parameters:
  - name: enableBuildDebug
    displayName: 'Enable build debug'
    type: boolean
    default: true

  - name: vmImage
    displayName: 'Pool VM image'
    type: string
    default: 'ubuntu-latest'
    values:
      - windows-latest
      - ubuntu-latest
      - macOS-latest

  - name: workingDirectory
    displayName: 'Working directory'
    type: string
    default: '$(Build.SourcesDirectory)/.azuredevops/dummy-projects/dotnet/dotnet-core'

trigger:
  batch: true
  paths:
    include:
      - .azuredevops/test/dotnet/dotnet/format.test.yml
      - dotnet/dotnet/install-SDK.yml
      - dotnet/dotnet/restore.yml
      - dotnet/dotnet/format.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

pr:
  branches:
    include:
      - '*'
  paths:
    include:
      - .azuredevops/test/dotnet/dotnet/format.test.yml
      - dotnet/dotnet/install-SDK.yml
      - dotnet/dotnet/restore.yml
      - dotnet/dotnet/format.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

schedules:
  - cron: '0 0 1 * *'
    displayName: 'Monthly check'
    branches:
      include:
        - main
    always: true

jobs:
  - job: DotnetFormat
    displayName: 'dotnet format'
    pool:
      vmImage: ${{ parameters.vmImage }}
    steps:
      - template: '../../../../dotnet/dotnet/install-SDK.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          workingDirectory: ${{ parameters.workingDirectory }}

      - template: '../../../../dotnet/dotnet/restore.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          workingDirectory: ${{ parameters.workingDirectory }}

      - template: '../../../../dotnet/dotnet/format.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          workingDirectory: ${{ parameters.workingDirectory }}

  - template: '../../../../.azuredevops/testHelpers/build-summary-validation.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      vmImage: ${{ parameters.vmImage }}
      dependsOn:
        - DotnetFormat
      expectedBuildResult: 'Succeeded'
      buildResultDescription: |
        Job **'dotnet format'** result must be: **Succeeded**
