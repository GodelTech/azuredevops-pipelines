# test-dotnet-build.job.yml

parameters:
  - name: enableBuildDebug
    displayName: 'Enable build debug'
    type: boolean
    default: true

  - name: vmImage
    displayName: 'Pool VM image'
    type: string
    default: 'ubuntu-latest'
    values:
      - windows-latest
      - ubuntu-latest
      - macOS-latest

  - name: sonarCloud
    displayName: 'SonarCloud parameters'
    type: object
    # prettier-ignore
    default: {
      enable: false
    }

  - name: dotnet
    displayName: '.NET parameters'
    type: object
    default: {}

trigger:
  batch: true
  paths:
    include:
      - .azuredevops/test/dotnet/build.job.yml
      - dotnet/build.job.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

schedules:
  - cron: '0 0 1 * *'
    displayName: 'Monthly check'
    branches:
      include:
        - main
    always: true

jobs:
  - template: '../../../dotnet/build.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      jobName: BuildDotnetDefaults
      jobDisplayName: 'Build .NET with defaults'
      vmImage: ${{ parameters.vmImage }}

  - template: '../../../dotnet/build.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      jobName: BuildDotnetDummyProject
      jobDisplayName: 'Build .NET dummy project'
      vmImage: ${{ parameters.vmImage }}
      sonarCloud: ${{ parameters.sonarCloud }}
      dotnet: ${{ parameters.dotnet }}

  - template: '../../../.azuredevops/testHelpers/build-summary-validation.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      vmImage: ${{ parameters.vmImage }}
      dependsOn:
        - BuildDotnetDefaults
        - BuildDotnetDummyProject
      expectedBuildResult: 'Succeeded'
      buildResultDescription: |
        Job **'Build .NET with defaults'**
        Step **'Use .NET SDK'** result must be: **Succeeded**

        Job **'Build .NET dummy project'**
        Step **'Use .NET SDK'** result must be: **Succeeded**
