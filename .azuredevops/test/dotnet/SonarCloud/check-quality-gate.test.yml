# test-dotnet-SonarCloud-check-quality-gate.test.yml

parameters:
  - name: enableBuildDebug
    displayName: 'Enable build debug'
    type: boolean
    default: true

  - name: vmImage
    displayName: 'Pool VM image'
    type: string
    default: 'ubuntu-latest'
    values:
      - windows-latest
      - ubuntu-latest
      - macOS-latest

  - name: serviceConnection
    displayName: 'SonarCloud service connection name'
    type: string
    default: 'SonarCloud'

  - name: organization
    displayName: 'SonarCloud organization'
    type: string
    default: 'godeltech'

  - name: projectKey
    displayName: 'SonarCloud project key'
    type: string
    default: 'GodelTech_azuredevops-pipelines_test_dotnet_SonarCloud_check-quality-gate'

  - name: projectName
    displayName: 'SonarCloud project name'
    type: string
    default: 'azuredevops-pipelines test dotnet SonarCloud check-quality-gate'

  - name: workingDirectory
    displayName: 'Working directory'
    type: string
    default: '$(Build.SourcesDirectory)/.azuredevops/dummy-projects/dotnet/dotnet-core'

trigger:
  batch: true
  paths:
    include:
      - .azuredevops/test/dotnet/SonarCloud/check-quality-gate.test.yml
      - dotnet/SonarCloud/check-quality-gate.yml
      - dotnet/SonarCloud/prepare.yml
      - dotnet/SonarCloud/analyze.yml
      - dotnet/dotnet/restore-and-build.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

pr:
  branches:
    include:
      - '*'
  paths:
    include:
      - .azuredevops/test/dotnet/SonarCloud/check-quality-gate.test.yml
      - dotnet/SonarCloud/check-quality-gate.yml
      - dotnet/SonarCloud/prepare.yml
      - dotnet/SonarCloud/analyze.yml
      - dotnet/dotnet/restore-and-build.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

schedules:
  - cron: '0 0 1 * *'
    displayName: 'Monthly check'
    branches:
      include:
        - main
    always: true

jobs:
  - job: CheckQualityGate
    displayName: 'Check quality gate'
    pool:
      vmImage: ${{ parameters.vmImage }}
    steps:
      - template: '../../../../dotnet/SonarCloud/prepare.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          enable: true
          serviceConnection: ${{ parameters.serviceConnection }}
          organization: ${{ parameters.organization }}
          projectKey: ${{ parameters.projectKey }}
          projectName: ${{ parameters.projectName }}
          workingDirectory: ${{ parameters.workingDirectory }}

      - template: '../../../../dotnet/dotnet/restore-and-build.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          workingDirectory: ${{ parameters.workingDirectory }}

      - template: '../../../../dotnet/SonarCloud/analyze.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          enable: true

      - template: '../../../../dotnet/SonarCloud/check-quality-gate.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          enable: true
          serviceConnection: ${{ parameters.serviceConnection }}
          organization: ${{ parameters.organization }}

  - template: '../../../../.azuredevops/testHelpers/build-summary-validation.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      vmImage: ${{ parameters.vmImage }}
      dependsOn:
        - CheckQualityGate
      expectedBuildResult: 'Succeeded'
      buildResultDescription: |
        Job **'Check quality gate'**
        Step **'Break build on quality gate failure'** result must be: **Succeeded**
