# test-dotnet-SonarCloud-prepare.test.yml

parameters:
  - name: enableBuildDebug
    displayName: 'Enable build debug'
    type: boolean
    default: true

  - name: vmImage
    displayName: 'Pool VM image'
    type: string
    default: 'ubuntu-latest'
    values:
      - windows-latest
      - ubuntu-latest
      - macOS-latest

  - name: serviceConnection
    displayName: 'SonarCloud service connection name'
    type: string
    default: 'SonarCloud'

  - name: organization
    displayName: 'SonarCloud organization'
    type: string
    default: 'godeltech'

  - name: projectKey
    displayName: 'SonarCloud project key'
    type: string
    default: 'GodelTech_azuredevops-pipelines_test_dotnet_SonarCloud_prepare'

  - name: projectName
    displayName: 'SonarCloud project name'
    type: string
    default: 'azuredevops-pipelines test dotnet SonarCloud prepare'

  - name: coverageReportPath
    displayName: 'Coverage report path'
    type: string
    default: '$(Agent.TempDirectory)/**/coverage.opencover.xml'

  - name: workingDirectory
    displayName: 'Working directory'
    type: string
    default: '$(Build.SourcesDirectory)/.azuredevops/dummy-projects/dotnet/dotnet-core'

trigger:
  batch: true
  paths:
    include:
      - .azuredevops/test/dotnet/SonarCloud/prepare.test.yml
      - dotnet/SonarCloud/prepare.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

pr:
  branches:
    include:
      - '*'
  paths:
    include:
      - .azuredevops/test/dotnet/SonarCloud/prepare.test.yml
      - dotnet/SonarCloud/prepare.yml
      - .azuredevops/testHelpers/build-summary-validation.job.yml

schedules:
  - cron: '0 0 1 * *'
    displayName: 'Monthly check'
    branches:
      include:
        - main
    always: true

jobs:
  - job: PrepareSonarCloudDisabled
    displayName: 'Prepare SonarCloud disabled'
    pool:
      vmImage: ${{ parameters.vmImage }}
    steps:
      - checkout: none

      - template: '../../../../dotnet/SonarCloud/prepare.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          enable: false

  - job: PrepareSonarCloudWithCustomParameters
    displayName: 'Prepare SonarCloud with custom parameters'
    pool:
      vmImage: ${{ parameters.vmImage }}
    steps:
      - checkout: none

      - template: '../../../../dotnet/SonarCloud/prepare.yml'
        parameters:
          enableBuildDebug: ${{ parameters.enableBuildDebug }}
          enable: true
          serviceConnection: ${{ parameters.serviceConnection }}
          organization: ${{ parameters.organization }}
          projectKey: ${{ parameters.projectKey }}
          projectName: ${{ parameters.projectName }}
          coverageReportPath: ${{ parameters.coverageReportPath }}
          workingDirectory: ${{ parameters.workingDirectory }}

  - template: '../../../../.azuredevops/testHelpers/build-summary-validation.job.yml'
    parameters:
      enableBuildDebug: ${{ parameters.enableBuildDebug }}
      vmImage: ${{ parameters.vmImage }}
      dependsOn:
        - PrepareSonarCloudDisabled
        - PrepareSonarCloudWithCustomParameters
      expectedBuildResult: 'Succeeded'
      buildResultDescription: |
        Job **'Prepare SonarCloud disabled'**
        Step **'Prepare SonarCloud analysis'** result must be: **Skipped**

        Job **'Prepare SonarCloud with custom parameters'**
        Step **'Prepare SonarCloud analysis'** result must be: **Succeeded**
